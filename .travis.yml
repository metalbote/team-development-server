os: linux
dist: xenial

language: bash

sudo: required

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

before_install:
  - sudo apt-get update

install:
  - bash init-environment.sh

script:
  - bash test-environment.sh

after_script:
  - bash destroy-environment.sh


